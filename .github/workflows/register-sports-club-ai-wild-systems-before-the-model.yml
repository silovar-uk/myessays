name: Register Sports Club AI Wild Systems Essay

on:
  push:
    paths:
      - '.github/workflows/register-sports-club-ai-wild-systems-before-the-model.yml'

permissions:
  contents: write

jobs:
  register:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Register canonical essay and English Mix
        run: |
          node <<'NODE'
          const fs = require('fs');
          const essayPath = 'essays/2026-09-16-sports-club-ai-wild-systems-before-the-model.md';
          const id = 'sports-club-ai-wild-systems-before-the-model';
          const mixPath = 'english-mix/sports-club-ai-wild-systems-before-the-model.md';

          const indexPath = 'data/index.json';
          const index = JSON.parse(fs.readFileSync(indexPath, 'utf8'));
          index.essays = index.essays.filter((p) => p !== essayPath);
          index.essays.unshift(essayPath);
          fs.writeFileSync(indexPath, JSON.stringify(index));

          const versionsPath = 'data/versions-index.json';
          const versions = JSON.parse(fs.readFileSync(versionsPath, 'utf8'));
          versions.articles ||= {};
          const existing = versions.articles[id] || {};
          const nextArticle = { ...existing, 'en-mix': mixPath };
          versions.articles = {
            [id]: nextArticle,
            ...Object.fromEntries(Object.entries(versions.articles).filter(([key]) => key !== id))
          };
          fs.writeFileSync(versionsPath, JSON.stringify(versions));

          JSON.parse(fs.readFileSync(indexPath, 'utf8'));
          JSON.parse(fs.readFileSync(versionsPath, 'utf8'));
          console.log('Registration JSON validated.');
          NODE
      - name: Validate reading-mode structure
        run: node scripts/validate-version-structure.mjs --id sports-club-ai-wild-systems-before-the-model
      - name: Remove one-shot workflow and commit registration
        run: |
          git rm .github/workflows/register-sports-club-ai-wild-systems-before-the-model.yml
          if git diff --cached --quiet && git diff --quiet -- data/index.json data/versions-index.json; then
            echo 'Nothing to commit.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add data/index.json data/versions-index.json
          git commit -m 'スポーツクラブAI予習記事を登録'
          git push
