name: Register Shuzo Nakamura Essay

on:
  push:
    paths:
      - '.github/workflows/register-shuzo-nakamura-essay.yml'

permissions:
  contents: write

jobs:
  register:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Update indexes
        shell: python
        run: |
          import json
          from pathlib import Path

          article_path = 'essays/2026-09-16-shuzo-nakamura-urawa-gm-person-and-system.md'
          article_id = 'shuzo-nakamura-urawa-gm-person-and-system'
          mix_path = 'english-mix/shuzo-nakamura-urawa-gm-person-and-system.md'

          p = Path('data/index.json')
          data = json.loads(p.read_text(encoding='utf-8'))
          essays = data['essays']
          if article_path not in essays:
              essays.insert(0, article_path)
          p.write_text(json.dumps(data, ensure_ascii=False, separators=(',', ':')), encoding='utf-8')

          p = Path('data/versions-index.json')
          data = json.loads(p.read_text(encoding='utf-8'))
          articles = data['articles']
          entry = articles.setdefault(article_id, {})
          entry['en-mix'] = mix_path
          p.write_text(json.dumps(data, ensure_ascii=False, separators=(',', ':')), encoding='utf-8')
      - name: Validate version structure
        run: node scripts/validate-version-structure.mjs --id shuzo-nakamura-urawa-gm-person-and-system
      - name: Commit registration
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/index.json data/versions-index.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "中村修三記事を登録"
          git push
