name: Register Future Self Freedom Essay

on:
  push:
    paths:
      - '.github/workflows/register-ai-remembers-likes-future-self-freedom.yml'

permissions:
  contents: write

jobs:
  register:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Register essay and English Mix version
        run: |
          node <<'NODE'
          const fs = require('fs');
          const id = 'ai-remembers-likes-future-self-freedom';
          const essay = 'essays/2026-09-15-ai-remembers-likes-future-self-freedom.md';
          const mix = 'english-mix/ai-remembers-likes-future-self-freedom.md';

          const indexPath = 'data/index.json';
          const index = JSON.parse(fs.readFileSync(indexPath, 'utf8'));
          index.essays = index.essays.filter((p) => p !== essay);
          index.essays.unshift(essay);
          fs.writeFileSync(indexPath, JSON.stringify(index));

          const versionsPath = 'data/versions-index.json';
          const versions = JSON.parse(fs.readFileSync(versionsPath, 'utf8'));
          versions.articles ||= {};
          versions.articles = {
            [id]: { ...(versions.articles[id] || {}), 'en-mix': mix },
            ...Object.fromEntries(Object.entries(versions.articles).filter(([key]) => key !== id))
          };
          fs.writeFileSync(versionsPath, JSON.stringify(versions));

          JSON.parse(fs.readFileSync(indexPath, 'utf8'));
          JSON.parse(fs.readFileSync(versionsPath, 'utf8'));
          NODE

      - name: Validate article structure
        run: node scripts/validate-version-structure.mjs --id ai-remembers-likes-future-self-freedom

      - name: Commit registration
        run: |
          if git diff --quiet -- data/index.json data/versions-index.json; then
            echo 'Already registered.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add data/index.json data/versions-index.json
          git commit -m 'AI推薦と未来の自己の記事をライブラリへ登録'
          git push
