name: Register TMS Hidden Profile and Catalyst

on:
  push:
    paths:
      - '.github/workflows/register-tms-hidden-profile-catalyst.yml'

permissions:
  contents: write

jobs:
  register:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Register canonical essays and English Mix versions
        run: |
          node <<'NODE'
          const fs = require('fs');
          const items = [
            {
              id: 'transactive-memory-hidden-profile-common-information',
              essay: 'essays/2026-09-15-transactive-memory-hidden-profile-common-information.md',
              mix: 'english-mix/transactive-memory-hidden-profile-common-information.md'
            },
            {
              id: 'transactive-memory-knowledge-catalyst-expertise-map',
              essay: 'essays/2026-09-15-transactive-memory-knowledge-catalyst-expertise-map.md',
              mix: 'english-mix/transactive-memory-knowledge-catalyst-expertise-map.md'
            }
          ];

          const indexPath = 'data/index.json';
          const index = JSON.parse(fs.readFileSync(indexPath, 'utf8'));
          for (const { essay } of items) index.essays = index.essays.filter((p) => p !== essay);
          index.essays.unshift(...items.map((x) => x.essay));
          fs.writeFileSync(indexPath, JSON.stringify(index));

          const versionsPath = 'data/versions-index.json';
          const versions = JSON.parse(fs.readFileSync(versionsPath, 'utf8'));
          versions.articles ||= {};
          const additions = {};
          for (const { id, mix } of items) additions[id] = { ...(versions.articles[id] || {}), 'en-mix': mix };
          versions.articles = {
            ...additions,
            ...Object.fromEntries(Object.entries(versions.articles).filter(([id]) => !items.some((x) => x.id === id)))
          };
          fs.writeFileSync(versionsPath, JSON.stringify(versions));
          JSON.parse(fs.readFileSync(indexPath, 'utf8'));
          JSON.parse(fs.readFileSync(versionsPath, 'utf8'));
          NODE

      - name: Validate hidden profile structure
        run: node scripts/validate-version-structure.mjs --id transactive-memory-hidden-profile-common-information

      - name: Validate catalyst structure
        run: node scripts/validate-version-structure.mjs --id transactive-memory-knowledge-catalyst-expertise-map

      - name: Commit registration
        run: |
          if git diff --quiet -- data/index.json data/versions-index.json; then
            echo 'Already registered.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add data/index.json data/versions-index.json
          git commit -m 'TMS続編2本をライブラリへ登録'
          git push
